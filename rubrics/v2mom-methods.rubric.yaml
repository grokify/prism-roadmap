# V2MOM Methods Rubric
# LLM-as-a-Judge evaluation criteria for V2MOM Methods documents
# Methods are the concrete initiatives that achieve the vision

id: v2mom-methods-rubric
name: V2MOM Methods Evaluation Rubric
version: "1.0"
description: |
  Evaluates V2MOM Methods documents for actionable definitions with owners and
  testable success criteria, traceability to values, ProductContext entities,
  and parent methods, and explicit dependency management.

passCriteria:
  scoreThresholds:
    pass: 80
    partial: 60

categories:
  - name: Method Definition
    weight: 40
    description: Evaluates each method's description, ownership, and success criteria
    criteria:
      - id: MD1
        name: Actionability
        weight: 15
        pass:
          description: Each method describes specific, concrete work
          indicators:
            - Descriptions state what will be done, not aspirations
            - Scope of each method is bounded
            - No empty or placeholder method slots
        partial:
          description: Methods described but fuzzy
          indicators:
            - Outcome language without the initiative behind it
            - Unbounded or overlapping scopes
            - Some methods placeholder
        fail:
          description: Methods missing or vague
          indicators:
            - Empty method sections
            - Methods restate the vision
      - id: MD2
        name: Ownership & Priority
        weight: 12
        pass:
          description: Every method has an owner, priority, and status
          indicators:
            - Named owner per method
            - Priority assigned (P0/P1/P2)
            - Status uses the defined states
        partial:
          description: Ownership incomplete
          indicators:
            - Owners for some methods only
            - Priorities missing or all identical
        fail:
          description: No ownership
          indicators:
            - No owners assigned
            - No priorities or statuses
      - id: MD3
        name: Success Criteria
        weight: 13
        pass:
          description: Each method has testable success criteria
          indicators:
            - Criteria are observable and checkable
            - Criteria cover the method's core outcome
            - Every method has at least one criterion
        partial:
          description: Criteria present but weak
          indicators:
            - Vague criteria (improved, better)
            - Criteria missing for some methods
        fail:
          description: No success criteria
          indicators:
            - Criteria checklists empty
            - Success undefined for all methods

  - name: Traceability & Alignment
    weight: 35
    description: Evaluates links to values, ProductContext entities, and parent methods
    criteria:
      - id: TA1
        name: Value Alignment
        weight: 10
        pass:
          description: Each method identifies which values it embodies
          indicators:
            - Value alignment stated per method
            - Cited values exist in the Values document
        partial:
          description: Value alignment thin
          indicators:
            - Alignment for some methods only
            - Values cited generically
        fail:
          description: No value alignment
          indicators:
            - Value alignment sections empty
      - id: TA2
        name: ProductContext Links
        weight: 12
        pass:
          description: Methods link to capabilities, projects, and requests
          indicators:
            - Link tables populated with real IDs
            - Relationship types stated
            - Metadata block mirrors the links
        partial:
          description: Links present but sparse
          indicators:
            - Some link tables empty
            - IDs without relationship context
        fail:
          description: No entity links
          indicators:
            - All link tables empty
            - Metadata productcontext arrays empty with no rationale
      - id: TA3
        name: Parent Method Alignment
        weight: 13
        pass:
          description: Non-company levels map each method to a parent method with contribution
          indicators:
            - Supports and Contribution filled per method (or explicit N/A for company level)
            - Mapped parent methods exist
            - Contribution states what this method delivers to the parent
        partial:
          description: Parent mapping incomplete
          indicators:
            - Parent named without contribution
            - Mapping for some methods only
        fail:
          description: No parent mapping
          indicators:
            - Sections empty for a non-company level
            - Mapped parent methods do not exist

  - name: Dependencies & Sequencing
    weight: 25
    description: Evaluates cross-team dependencies and the timeline
    criteria:
      - id: DS1
        name: Cross-Team Dependencies
        weight: 13
        pass:
          description: Dependencies on other teams are identified with status
          indicators:
            - Dependency table names the method, dependency, team, and status
            - High-risk dependencies visible
            - Explicit statement when no dependencies exist
        partial:
          description: Dependencies acknowledged but untracked
          indicators:
            - Dependencies listed without team or status
            - Table partially filled
        fail:
          description: Dependencies ignored
          indicators:
            - Dependency table empty with no statement
            - Known cross-team work absent
      - id: DS2
        name: Sequencing
        weight: 12
        pass:
          description: Methods have target dates and a coherent timeline
          indicators:
            - Target dates per method
            - Timeline/sequence reflects dependency order
            - Sequencing consistent with priorities
        partial:
          description: Timeline present but inconsistent
          indicators:
            - Dates for some methods only
            - Sequence conflicts with stated dependencies
        fail:
          description: No sequencing
          indicators:
            - No target dates
            - Timeline section empty
